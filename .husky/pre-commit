#!/usr/bin/env sh
# Pre-commit hook - run lint-staged
# Use here-doc to avoid CRLF line ending issues
# Change to git repo root where package.json is located
cd "$(git rev-parse --show-toplevel 2>/dev/null)" 2>/dev/null || true
sh <<'ENDOFSCRIPT'
npx lint-staged
ENDOFSCRIPT
